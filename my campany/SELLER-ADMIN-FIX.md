Seller administration is now linked end-to-end:
Application -> Admin Review -> Approve -> Seller record ACTIVE -> SELLER role.
Only one open application is permitted per profile by a partial unique index.
Older duplicate pending applications were rejected as duplicates; the newest application remains pending.
